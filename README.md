# Board Game Framework

Domain-first Kotlin contracts for immutable game state, legal player intents, event resolution, turn flow, outcomes, history, boards, players, and optional game configuration.

```bash
mvn test
mvn spotless:apply
mvn verify
```
